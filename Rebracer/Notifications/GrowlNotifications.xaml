<Window x:Class="WpfGrowlNotification.GrowlNotifications"
		xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
		xmlns:wcp="clr-namespace:Microsoft.VisualStudio.PlatformUI.Shell.Controls;assembly=Microsoft.VisualStudio.Shell.ViewManager"
		xmlns:shellui="clr-namespace:Microsoft.VisualStudio.PlatformUI;assembly=Microsoft.VisualStudio.Shell.12.0"
		xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:Model="clr-namespace:WpfGrowlNotification"
		xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
		xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d"
		Height="530" Width="300"
		ShowActivated="False" AllowsTransparency="True" ShowInTaskbar="False" 
		WindowStyle="None" Background="Transparent" Topmost="True" UseLayoutRounding="True">
	<Window.Resources>
		<Storyboard x:Key="CollapseStoryboard">
			<DoubleAnimation From="100" To="0" Storyboard.TargetProperty="Height" Duration="0:0:1"/>
		</Storyboard>
		<DataTemplate x:Key="MessageTemplate" DataType="Model:Notification">
			<Border x:Name="NotificationWindow" SizeChanged="NotificationWindowSizeChanged"
						Background="{DynamicResource {x:Static shellui:EnvironmentColors.ComboBoxMouseOverBackgroundGradientBrushKey}}"
						BorderBrush="{DynamicResource {x:Static shellui:EnvironmentColors.ComboBoxMouseOverBorderBrushKey}}"
						BorderThickness="1" Margin="10">
				<Grid Height="100" Width="280" Margin="6">
					<Grid.RowDefinitions>
						<RowDefinition Height="Auto"></RowDefinition>
						<RowDefinition Height="*"></RowDefinition>
					</Grid.RowDefinitions>
					<Grid.ColumnDefinitions>
						<ColumnDefinition Width="Auto"></ColumnDefinition>
						<ColumnDefinition Width="*"></ColumnDefinition>
					</Grid.ColumnDefinitions>
					<Image Grid.RowSpan="2" Source="{Binding Path=ImageUrl}" Margin="4" Width="80"></Image>
					<TextBlock Grid.Column="1" Text="{Binding Path=Title}"  TextOptions.TextRenderingMode="ClearType" TextOptions.TextFormattingMode="Display" Foreground="{DynamicResource {x:Static shellui:EnvironmentColors.ComboBoxMouseOverTextBrushKey}}" 
								   FontFamily="Arial" FontSize="14" FontWeight="Bold" VerticalAlignment="Center"  Margin="2,4,4,2" TextWrapping="Wrap" TextTrimming="CharacterEllipsis" />
					<wcp:WindowTitleBarButton x:Name="CloseButton" Grid.Column="1" 
												  HorizontalAlignment="Right"
												  BorderBrush="{DynamicResource {x:Static shellui:EnvironmentColors.MainWindowButtonActiveBorderBrushKey}}" 
												  BorderThickness="1,0,1,1"
												  GlyphForeground="{DynamicResource {x:Static shellui:EnvironmentColors.ToolWindowButtonActiveGlyphBrushKey}}" 
												  HoverBackground="{DynamicResource {x:Static shellui:EnvironmentColors.ToolWindowButtonHoverActiveBrushKey}}" 
												  HoverBorderBrush="{DynamicResource {x:Static shellui:EnvironmentColors.ToolWindowButtonHoverActiveBorderBrushKey}}"
												  HoverForeground="{DynamicResource {x:Static shellui:EnvironmentColors.ToolWindowButtonHoverActiveGlyphBrushKey}}" 
												  HoverBorderThickness="1,0,1,1"
												  PressedBackground="{DynamicResource {x:Static shellui:EnvironmentColors.ToolWindowButtonDownBrushKey}}" 
												  PressedBorderBrush="{DynamicResource {x:Static shellui:EnvironmentColors.ToolWindowButtonDownBorderBrushKey}}" 
												  PressedForeground="{DynamicResource {x:Static shellui:EnvironmentColors.ToolWindowButtonDownActiveGlyphBrushKey}}" 
												  PressedBorderThickness="1,0,1,1" Padding="8" 
												  AutomationProperties.Name="Close" AutomationProperties.AutomationId="Close" 
												  ToolTip="Close">
						<Path Name="HideButtonPath" x:Uid="HideButtonPath" Width="10" Height="8" Stretch="Uniform" Data="F1M0,0L2,0 5,3 8,0 10,0 6,4 10,8 8,8 5,5 2,8 0,8 4,4 0,0z" 
								  Fill="{Binding Path=(TextElement.Foreground), RelativeSource={RelativeSource Self}}" />
					</wcp:WindowTitleBarButton>

					<TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Path=Message}"  TextOptions.TextRenderingMode="ClearType" TextOptions.TextFormattingMode="Display" Foreground="{DynamicResource {x:Static shellui:EnvironmentColors.ComboBoxMouseOverTextBrushKey}}" 
								   FontFamily="Arial" VerticalAlignment="Center"  Margin="2,2,4,4" TextWrapping="Wrap" TextTrimming="CharacterEllipsis"/>
				</Grid>
			</Border>
			<DataTemplate.Triggers>
				<EventTrigger RoutedEvent="Window.Loaded" SourceName="NotificationWindow">
					<BeginStoryboard x:Name="FadeInStoryBoard">
						<Storyboard>
							<DoubleAnimation Storyboard.TargetName="NotificationWindow" From="0.01" To="1" Storyboard.TargetProperty="Opacity" Duration="0:0:2"/>
							<DoubleAnimation Storyboard.TargetName="NotificationWindow" From="1" To="0" Storyboard.TargetProperty="Opacity" Duration="0:0:2" BeginTime="0:0:6"/>
						</Storyboard>
					</BeginStoryboard>
				</EventTrigger>
				<Trigger Property="IsMouseOver" Value="True">
					<Trigger.EnterActions>
						<SeekStoryboard Offset="0:0:3" BeginStoryboardName="FadeInStoryBoard" />
						<PauseStoryboard BeginStoryboardName="FadeInStoryBoard" />
					</Trigger.EnterActions>
					<Trigger.ExitActions>
						<SeekStoryboard Offset="0:0:3" BeginStoryboardName="FadeInStoryBoard" />
						<ResumeStoryboard BeginStoryboardName="FadeInStoryBoard"></ResumeStoryboard>
					</Trigger.ExitActions>
				</Trigger>
				<EventTrigger RoutedEvent="Button.Click" SourceName="CloseButton">
					<BeginStoryboard>
						<Storyboard >
							<DoubleAnimation Storyboard.TargetName="NotificationWindow" From="1" To="0" Storyboard.TargetProperty="(Grid.Opacity)" Duration="0:0:0"/>
						</Storyboard>
					</BeginStoryboard>
				</EventTrigger>
				<Trigger SourceName="NotificationWindow" Property="Opacity" Value="0">
					<Setter TargetName="NotificationWindow" Property="Visibility" Value="Hidden"></Setter>
					<Trigger.EnterActions>
						<BeginStoryboard Storyboard="{StaticResource CollapseStoryboard}"/>
					</Trigger.EnterActions>
				</Trigger>
			</DataTemplate.Triggers>
		</DataTemplate>
	</Window.Resources>
	<ItemsControl x:Name="NotificationsControl" FocusVisualStyle="{x:Null}" d:DataContext="{d:DesignData Source=DesignTimeNotificationData.xaml}" ItemsSource="{Binding .}" ItemTemplate="{StaticResource MessageTemplate}" />
</Window>
